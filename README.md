# Comandos para rodar no Inicio

- 'npm install'
- 'composer install'
- Para rodar o servidor 'php artisan serve' 

# Comandos GIT

- git clone 'link do repositório'
- git config --global user.name 'nome'
- git config --global user.email 'email'